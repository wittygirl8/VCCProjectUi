import React from 'react';
import './AddData.css'; // Import the CSS file

function AddData() {
  return (
    <div>
      <h1>Add Data</h1>
      {/* Add the form or input fields to add data */}
    </div>
  );
}

export default AddData;
